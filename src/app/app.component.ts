import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'app';

  // declare two number

  // create another variable thats the sum

  // create a method getLargest() that returns the largest value of an array of numbers...

  // use {{ }} to bind to the result i.e. {{ getLargest([num1, num2, etc...]) }}
}
